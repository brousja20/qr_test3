<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="qr_test3.NewPage2"
             Title="NewPage2">
    <VerticalStackLayout>
        <Label 
            x:Name="SettingsLabel"
            Text="Settings 🤙"
            VerticalOptions="Center" 
            HorizontalOptions="Center" 
            Margin="10"
            FontSize="20"
            />
        <Label
            x:Name="ThemeLabel"
            VerticalOptions="Center" 
            HorizontalOptions="Center"
            Margin="10"
        >Theme</Label>
        <Button 
            x:Name="ThemeButton"
            Text="Toggle" 
            WidthRequest="75" 
            Margin="10"
            Clicked="Change_Theme"
            ></Button>
    </VerticalStackLayout>
</ContentPage>